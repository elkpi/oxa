[package]
name = "oxa-sse"
description = "Standalone byte-level Server-Sent Events (SSE) frame adapter (spec/20 §6)"
edition.workspace = true
version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
