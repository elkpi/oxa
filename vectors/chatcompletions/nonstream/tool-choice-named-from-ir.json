{
  "name": "chatcompletions.nonstream.tool-choice-named-from-ir",
  "description": "IR tool-choice mode tool with a name renders as a named Chat Completions tool_choice {type:function, function:{name}}.",
  "spec_version": "0.1.0",
  "mode": "nonstream",
  "conversion": "from-ir",
  "source": { "protocol": "chatcompletions" },
  "target": { "protocol": "chatcompletions" },
  "input": {
    "specVersion": "0.1.0",
    "model": "gpt-4o-mini",
    "messages": [{ "role": "user", "content": [{ "type": "text", "text": "Weather in Paris?" }] }],
    "tools": [
      {
        "name": "get_weather",
        "input_schema": { "type": "object", "properties": { "city": { "type": "string" } } }
      }
    ],
    "tool_choice": { "mode": "tool", "name": "get_weather" }
  },
  "expected_output": {
    "model": "gpt-4o-mini",
    "messages": [{ "role": "user", "content": "Weather in Paris?" }],
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "get_weather",
          "parameters": { "type": "object", "properties": { "city": { "type": "string" } } }
        }
      }
    ],
    "tool_choice": { "type": "function", "function": { "name": "get_weather" } }
  },
  "expected_losses": [],
  "tags": ["tools", "request", "tool-choice", "n-cc-7"]
}
