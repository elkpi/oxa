{
  "name": "chatcompletions.nonstream.loss-parallel-tool-calls-to-ir",
  "description": "Chat Completions parallel_tool_calls has no IR equivalent in v1 and is dropped with an unmapped-field loss.",
  "spec_version": "0.1.0",
  "mode": "nonstream",
  "conversion": "to-ir",
  "source": { "protocol": "chatcompletions" },
  "input": {
    "model": "gpt-4o-mini",
    "messages": [{ "role": "user", "content": "Weather in Paris?" }],
    "parallel_tool_calls": true
  },
  "expected_ir": {
    "specVersion": "0.1.0",
    "model": "gpt-4o-mini",
    "messages": [{ "role": "user", "content": [{ "type": "text", "text": "Weather in Paris?" }] }]
  },
  "expected_losses": [
    {
      "path": "parallel_tool_calls",
      "field": "parallel_tool_calls",
      "reason": "unmapped-field",
      "detail": "Chat Completions parallel tool calls have no IR equivalent in v1."
    }
  ],
  "tags": ["loss", "request", "tools"]
}
